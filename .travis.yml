language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dzmitryst/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dzmitryst/docker-react npm run test -- --coverage
